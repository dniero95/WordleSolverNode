// const { words } = require("../routes/word");

// const filter = (randomLetters, wrongLetters, correctLetters) => {
//     for (let word in words) {
//         for (let index = 0; index < wrongLetters.length; index++) {
//             const letter = wrongLetters[index];
//             if (word.includes(letter)) {
                
//             }  
//         }
//     }
// }

module.exports = filter;